<h1> New Photo</h1>

<%= form_for @photo, :url => photos_path, :html => { :multipart => true } do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.file_field :image %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
